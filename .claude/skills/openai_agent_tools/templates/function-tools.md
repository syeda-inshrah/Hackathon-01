Define OpenAI Agent tools using the function_tool decorator.

Guidelines:
- One tool = one responsibility
- Use clear, descriptive names
- Inputs must be explicit and typed
- Outputs must be structured and predictable

Each tool should:
1. Accept validated inputs
2. Perform retrieval, lookup, or storage
3. Return raw data (no interpretation)
