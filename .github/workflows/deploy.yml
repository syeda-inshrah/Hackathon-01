name: Deploy Docusaurus to GitHub Pages

on:
  push:
    # Action ab in dono branches par trigger hoga
    branches:
      - main            
      - 001-spec-kit-setup  

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  deploy:
    name: Deploy to GitHub Pages
    runs-on: ubuntu-latest
    
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          
      # Clean cache (Optional but recommended)
      - name: Clean npm cache (Pre-install safety)
        run: npm cache clean --force
        
      # [CRITICAL FIX] npm ci ko npm install se badal rahe hain taaki lock file error hal ho jaye.
      - name: Install dependencies (npm install --prefix)
        run: npm install --prefix Hackathon

      # Build Docusaurus using --prefix Hackathon
      - name: Build Docusaurus website
        run: npm run build --prefix Hackathon

      - name: Deploy to GitHub Pages
        id: deployment
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          # Build output ka path Hackathon folder ke andar set kiya
          publish_dir: ./Hackathon/build 
          publish_branch: gh-pages